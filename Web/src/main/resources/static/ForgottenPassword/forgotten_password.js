function sendSMS() {
    // SMS gönderme işlemleri burada gerçekleştirilebilir, örnek olarak API çağrısı yapılabilir.
    // Örnek olarak, bir mesaj gönderme işlemini taklit edelim:

    if(0){
        alert("SMS gönderildi! Lütfen gelen kodu giriniz.");
    }

    // SMS gönderildiğini varsayarak, Şifremi Sıfırla butonunu aktifleştiriyoruz.
    document.getElementById("resetButton").disabled = false;
}